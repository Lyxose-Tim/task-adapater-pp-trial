# Backward-compatible entry point. The preferred helper follows the requested
# environment name but automatically falls back to the original tsa_mlt name.
. (Join-Path $PSScriptRoot 'env_task_adapter_pp.ps1')
