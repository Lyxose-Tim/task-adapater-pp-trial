# 交接包⑧：M6 · 3b 消融表补点（λ=2,3；云端，Cursor 执行）

代号：**M6-3b-ablation** ｜ 产出：2026-09-02 ｜ 承接：Cursor → 云端 GPU

## 1. 目的与里程碑

**M6：消融表 + λ 曲线（验收三件套齐备，直接进论文）**。M5 已得 3 点
（B0 / λ=1 / λ=5），OS 单调 0.21→0.32→0.82，λ=1 Acc 无损、λ=5 降 1.6pt。
补 **λ=2,3** 两点，把 λ-OS / λ-Acc 曲线画满、定"知更点"（OS 最大化而 Acc 未显著降）。

## 2. 版本与环境

- 代码：`main` HEAD（3b 实现 `d6b8923`，未变；新增 `config_3b_lam2/3.yaml`）。`git pull`。
- 环境/数据/CLIP：同 M5（训练型，24GB 卡，~14.7GB）。

## 3. 执行步骤（与 M5 同结构，每 λ 一个闭环）

```bash
# λ=2
TA_CONFIG=config_3b_lam2.yaml python run.py 2>&1 | tee 3b_lam2_console.log
#   复诊断：复制 config_3a_final.yaml→config_3a_3b_lam2.yaml，checkpoint 改为上一步 best <acc>.tar
TA_CONFIG=config_3a_3b_lam2.yaml python diagnose.py 2>&1 | tee 3b_lam2_diag.log
# λ=3 同上（config_3b_lam3.yaml）
```
- 无需再跑 smoke（M5 已验 L_order 通路）。各 λ 约 3.7h 训练 + 3.7h 复诊，可串行。

## 4. 成功判据

- [ ] λ=2、3 各自训练 10 epoch 完成、终测 10000 ep 出数、复诊断出 OS（sanity 仍 true）；
- [ ] 记录训练 L_order 曲线、Acc、复诊 OS、分支 Acc。

## 5. 判读（Claude 收包后）

- 五点（B0/1/2/3/5）画 λ-OS 与 λ-Acc 曲线；定知更点（建议主配置）；
- 若 λ=2/3 落在"OS 明显>B0 且 Acc 未显著降"→ 作为 3b 主推荐 λ；
- 三件套消融表冻结进论文（{B0, +L_order(λ)} × {Acc, OS, 分支 Acc}）。

## 6. 需带回产物（放入 `experiments/returns/2026-09-02_M6-3b-ablation/{lam2,lam3}/`）

每 λ：训练 `log.txt`+`console.log`（含 L_order 曲线）、best `<acc>.tar`、终测 Acc、
复诊 `diagnose_3a_*.json`+`diag.log`、config 快照、`nvidia` 峰值与墙钟、异常全文。

收包后 Claude：五点入 ledger → λ-OS/λ-Acc 曲线 → 3b 结论与主推荐 λ → 创新点 3 收官（进创新点 1）。
