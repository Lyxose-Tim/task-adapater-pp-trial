# 交接包⑤：M3 · 3a 顺序敏感性诊断（云端，Cursor 执行）

代号：**M3-3a-dev** ｜ 产出：2026-08-31 ｜ 承接：Cursor → 云端 GPU

## 1. 目的与里程碑

**M3：诊断主表（开发规模 600–2500 episode）**。加载 B0 权重做**推理型**诊断（不训练），
测量 Task-Adapter++ 语义分支对子动作顺序的敏感度。回答手册 §2.5 的预注册三分支：
OS≈0（不敏感，3b 正则动机强）／ OS>0（敏感，可归因式(16) 阶段-窗口耦合）。

## 2. 版本与环境

- 代码：`git pull` 到 `main` @ **`fe63d69`**（含 `diagnose.py` + `models.py` 可复用重构 + `config_3a_dev.yaml`）。云端旧 clone：`git fetch && git reset --hard origin/main`。
- 环境：与 B0 同（torch 2.2.2+cu121、CLIP JIT 已装、`CLIP_VIT_B16_PATH` 已设、`data/full/` 已抽帧）。**推理型，不训练，无需重抽帧。**
- **B0 参照权重**：诊断加载 **seed 916 实例** 的 checkpoint（`56.32.tar`，terminal test 56.92，最接近 n=5 均值 57.01，作配对下游参照）。本地在 `experiments/returns/2026-08-30_B0_torch222/56.32.tar`（gitignore 未入库）——**上传到云端**，把 `config_3a_dev.yaml` 的 `checkpoint:` 字段改为其云端路径。

## 3. 执行命令（仓库根目录）

```bash
# 先确认 checkpoint 路径已在 config_3a_dev.yaml 填好（B0 seed916 的 56.32.tar）
TA_CONFIG=config_3a_dev.yaml python diagnose.py 2>&1 | tee diagnose_3a_dev.log
```

- 推理型：无反传，显存低于训练（预计 <10GB），墙钟远短于 B0（1000 episode，估 20–40min，待实测）。
- 结果落 `workspace_3a_dev/diagnose_3a/diagnose_3a_<时间戳>.json`；控制台每 200 episode 打印滚动 C0/OS。
- 条件矩阵（手册 §2.1）：C0 正序文本｜C1 反转(2,1,0)｜C2 全 5 非恒等排列｜C3 帧反转｜C4 帧随机打乱（种子 916+episode）。视觉前向对 C0/C1/C2 复用一次；C3/C4 各重做。

## 4. 成功判据（逐项勾选）

- [ ] 端到端无报错、完整跑完 1000 episode；
- [ ] **§2.2 sanity 通过**：`sanity_text_perturb_leaves_visual_unchanged: true`（文本扰动 C1/C2 下仅视觉分 Acc 逐 episode 不变——若 false，控制台会 WARNING，代码有 bug，需带回排查）；
- [ ] 输出含各条件 Acc(融合/仅语义/仅视觉)、**OS = Acc(C0)−mean Acc(C2) 及配对 95% CI**、C0−C1 配对差；
- [ ] 逐排列（C2 的 5 个）Acc 明细齐全。

## 5. 判读（Claude 收包后据手册 §2.5 定性，不预设结论）

- OS 的符号与量级 + CI 是否含 0 → 敏感/不敏感；
- 分支分解交叉验证：文本扰动应只动语义分支（sanity）；C3/C4（帧扰动）应主要动视觉/融合分；
- C1（反转）vs C2（均值）一致性；正确类语义 margin 分布（后续画图备料，可选二期）。

## 6. 需带回产物（放入 `experiments/returns/2026-08-31_M3-3a-dev/`）

1. `diagnose_3a_<时间戳>.json`（全部指标）；
2. `diagnose_3a_dev.log`（控制台，含滚动 OS 与 sanity 行）；
3. `nvidia-smi` 显存峰值与墙钟；
4. 实际 `config_3a_dev.yaml` 快照（含填好的 checkpoint 路径）；
5. 异常全文（若有）。

收包后 Claude：解析 JSON → 核 sanity → 报告 OS±CI 与预注册分支归属 → 写 ledger（OS 列）/PROGRESS → 决定 M4（10000 episode 终表）与 3b 走向。
