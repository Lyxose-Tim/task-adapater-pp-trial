# 交接包④：B0 补种子 seed 919/920（云端，Cursor 执行）

代号：**B0-seed2more** ｜ 产出：2026-08-31 ｜ 承接：Cursor → 云端 GPU

## 1. 目的

把 B0 定标从 3 种子扩到 **n=5**，收紧因 seed917 离群导致的 ±1.66 方差。
新增 seed 919/920（fixed × torch 2.2.2，10 epoch），与既有 916/917/918 同代码同协议，
最终 **B0 = 5 种子(916/917/918/919/920) mean±std**。仅补方差估计；下游创新点仍按手册用 seed 916 配对单种子测 Δ。

## 2. 版本与环境（⚠️ 结构已变）

- **仓库结构已对齐上游**：官方训练代码现在**仓库根目录**（`run.py`/`models.py`/`dataset.py`/`utils.py`/`module_adapter.py`/`module_sem_adapter.py`/`config*.yaml`/`corpus/`），原 `Task-Adapter-pp/` 文件夹已取消。`git pull` 到 `main` 最新（结构重构提交，哈希见 PROGRESS/ledger「B0 代码锁定」行）。**官方模型架构与初始化未变**（仍是训 916–918 那版：无 task_adapter 零初始化），B0 checkpoint 可正常加载，919/920 与前 3 种子同质。
- 环境：与前几跑同一云端（torch 2.2.2+cu121、CLIP JIT 已装、`CLIP_VIT_B16_PATH` 已设、数据 `data/full/` 已抽帧 6400/2400/2400）。**无需重装、无需重抽帧。**
- 若云端是旧 clone（含 Task-Adapter-pp/ 文件夹）：`git fetch && git reset --hard origin/main` 切到新结构（force-push 后历史已重写，用 reset --hard）。

## 3. 执行命令（两跑，根目录运行）

```bash
# 仓库根目录（不再 cd Task-Adapter-pp）
TA_CONFIG=config_b0_seed919.yaml python run.py 2>&1 | tee b0_s919_console.log
TA_CONFIG=config_b0_seed920.yaml python run.py 2>&1 | tee b0_s920_console.log
nvidia-smi --query-gpu=memory.used --format=csv -l 30 > nvidia_s919.csv   # 各跑并行采样
```

- 显存 ~14.7GB/跑，单跑约 3h；日志/ckpt 落 `workspace_b0_s919|s920/.../log.txt`。
- 断点续跑：改对应 config 的 `load_weights/checkpoint/start_epoch`。

## 4. 成功判据

- [ ] 两跑各 10 epoch 完成、loss 有限下降、无 NaN/Inf；
- [ ] 各跑终测 10000 episode 出数；
- [ ] 记录 nvidia 峰值与训练/终测墙钟。

## 5. 需带回产物（放入 `experiments/returns/2026-08-31_B0-seed2more/{s919,s920}/`）

各含：`log.txt` + `b0_s*_console.log`、best ckpt `<val>.tar`、`nvidia_s*.csv`、实际 config 快照、异常全文（若有）。

收包后 Claude：算 5 种子 mean±std → 写 ledger B0 定义行 → M2 验收结论 → 开工 M3（diagnose() + 交接包）。
