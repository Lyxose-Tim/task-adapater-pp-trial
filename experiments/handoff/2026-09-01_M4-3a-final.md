# 交接包⑥：M4 · 3a 诊断终表（10000 episode，云端，Cursor 执行）

代号：**M4-3a-final** ｜ 产出：2026-09-01 ｜ 承接：Cursor → 云端 GPU

## 1. 目的与里程碑

**M4：诊断终表**。把 3a 诊断从开发规模（1000 ep）放到 10000 ep，收紧配对 CI，
**定性归属手册 §2.5 的预注册分支**。M3 dev 结果 OS = 0.43 ± 0.41（CI [0.02, 0.84]
勉强不含 0）介于「OS≈0」与「显著>0」之间——10000 ep 预计把 OS 的 ±CI 收到 ~±0.13，
可定论。顺序效应集中在**语义分支**（dev：语义 OS≈0.51、C0−C1=0.96；融合被视觉稀释）。

## 2. 版本与环境

- 代码：`main` @ HEAD（含 `diagnose.py`、`config_3a_final.yaml`，与 M3 dev 同一 `fe63d69` 诊断实现）。`git pull` 到最新。
- 环境/数据/ckpt：与交接包⑤（M3 dev）完全相同——加载 **B0 seed916 的 `56.32.tar`**（已在云端；`config_3a_final.yaml` 的 `checkpoint:` 沿用 dev 填好的路径）。推理型不训练。

## 3. 执行命令（仓库根目录）

```bash
TA_CONFIG=config_3a_final.yaml python diagnose.py 2>&1 | tee diagnose_3a_final.log
```

- 与 dev 唯一差别：`diagnose_episodes: 10000`（其余协议、条件矩阵 C0–C4、种子完全一致）。
- 墙钟：dev 1000 ep 约 22.6 min（显存 1.6GB）→ 终表 10000 ep 估 **~3.5–4h**（线性外推，待实测）。
- 结果落 `workspace_3a_final/diagnose_3a/diagnose_3a_<时间戳>.json`。

## 4. 成功判据（逐项勾选）

- [ ] 完整跑完 10000 episode、无报错；
- [ ] **§2.2 sanity 仍 true**（文本扰动下仅视觉分逐 episode 不变）；
- [ ] OS = Acc(C0)−mean Acc(C2) 及配对 95% CI 出数（CI 应明显窄于 dev 的 ±0.41）；
- [ ] 融合/语义/视觉三分支各条件 Acc + C2 逐排列明细齐全。

## 5. 需带回产物（放入 `experiments/returns/2026-09-01_M4-3a-final/`）

`diagnose_3a_<时间戳>.json`、`diagnose_3a_final.log`、`nvidia` 峰值与墙钟、`config_3a_final.yaml` 快照、异常全文（若有）。

收包后 Claude：解析 → 核 sanity → **报告终表 OS±CI 并归属预注册分支**（§2.5）→ 写 ledger 终表行 → 决定 3b 是否开工（OS 显著>0 → 3b 增强角色；OS≈0 → 3b 修复角色、动机强）与《创新点1_执行清单》时点。
