# 阶段 E · 主图图注（交接包⑯）

对照 `experiments/handoff/2026-09-09_E-I1-plan-dump.md` §4 本地绘图项。
数据：seed 916 内容盲固定 12 episode；主图取其中**第一个含 pretend 类**的 episode（ep1，类 id 78），
query 取该类（q=3）。未挑样、未改划分。π 网格为 CrossAttention 后的 T=7（方案 §1.1 Option A），
横轴 7 列对应 8 段均匀采样的前 7 帧缩略图。

## Fig. i1e_heatmap_ABC（主图）

**A → B → C**：固定窗口（式 16）→ 纯内容 OT（λ=0）→ 内容+顺序先验 OT（λ=0.3，ρ=None）。
纵轴为 3 个语义阶段，横轴为 CA 帧；色深为 π(t,k)。12 episode GT 类带状质量 λ=0 为 **0.335**、λ=0.3 为 **0.554**（与探针一致）；
主图 ep1 q3 为 0.34 / 0.55。λ=0.3 更集中于位置带、但不是所有 query 共用同一掩码（见 twoquery 图）。

## Fig. i1e_heatmap_twoquery

同一 episode 的 pretend 类 query 与另一类 query。λ=0.3 两侧都更带状，形状不同 → 非退化为固定模板。

## Fig. i1e_rho1_stagemass（附录）

λ=0.3 上 ρ=1 相对 ρ=None：阶段质量偏离均匀 1/K（relaxation）。终表 Acc/OS 显示该放松不超过主线，故 ρ=1 只作附录。

## Fig. i1e_heatmap_seed917

同 12 episode 流、主线 λ=0.3，第二训练种子。机制形态一致；主图仍用 seed 916。

## 冻结沿用（非本包新训）

- `experiments/figures/i1b_lambda_curves.png`：λ-Acc / λ-OS
- `experiments/figures/i1c_truncation_dacc.png`：截断 ΔAcc
- `experiments/figures/i1_m9_final.png`：终表 4 行

## 透明例外（不能按字面重导）

手册要求 12 episode「含 3 个 pretend 类」。CMN 测试划分 24 类 ∩ pretend 词表 = {78}。
内容盲扩 episode 索引无法增加 pretend **类**的个数（不改划分、不挑样）。本 12 流已含该类（ep1/10/11）。
保持 dump_episodes=12。
