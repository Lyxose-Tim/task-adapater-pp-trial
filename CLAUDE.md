# CLAUDE.md — Task-Adapter++ 改进研究（FSAR 论文实验）

## 0. 项目定位
在 Task-Adapter++（arXiv:2505.06002，官方仓库 Jaulin-Bage/Task-Adapter-pp）基础上实现并验证改进方案。基准：SSv2-Small · 5-way 1-shot（CMN 划分）。最终产出中文论文。
创新点实施顺序固定：**3 → 1 → 2 → 4a**（4b 默认搁置）。

## 1. 运行环境与职责边界（先读这个）
本工作目录在**用户的本地电脑**上。本地具备：代码读写、CPU 单元测试、基于 mini 子集的**验证性训练（smoke，见 §8）**；不具备：完整数据集与正式训练评测所需算力。
- **Claude Code 的三类工作**：① 实施——写码、改码、单测、smoke；② 审查——按 §2 产出审查报告；③ 交接与登账——产出交接包、解析 returns、维护 ledger 与 PROGRESS。
- **正式（全量）训练与评测仍全部走云端交接包**；本地 smoke 不产生任何可入论文的数字。
- 本地下载许可（2026-07-18 决议修订）：本地实际具备可训练 GPU 与 SSv2 数据（`dataset/SSv2`＋`dataset/smsm_cmn` 标注），经用户认可留存，原"完整 SSv2 禁止进本地"条款废止；K400 已按决议删除。本地允许**小规模数据训练，仅用于验证代码有效性**（定位同 §8 smoke：产出不进 ledger、不进论文），以免代码带错上云浪费资源；**正式定标训练与评测协议不变**（走云端交接包；如后续决议改为本地正式训练，另行修订本条）。CLIP 权重已在 `dataset/checkpoints/`；mini 子集可由本地数据直接抽样，交接包①的"云端打包带回"步骤相应作废。
- 不要假设本地能跑通依赖完整数据的路径：mini 子集覆盖不到的验证项，标注"云端验证项"写入交接包。

## 2. 工作模式：实施 vs 审查
每个子任务处于两种模式之一，开工复述中必须声明当前模式。
**切换协议**：用户消息以「模式：实施」或「模式：审查」开头时显式切换；未声明时按措辞推断（"检查 / 审查 / 评估 / 优化建议 / review"→ 审查；"实现 / 编写 / 修改 / 落实 R-XX"→ 实施），推断结果写进复述供用户纠正。同一会话可切换，每次切换须重新声明。

**实施模式（Builder）**：按 §3–§9 正常读文档、写码、单测、smoke、发交接包。commit 若落实审查建议，message 中引用编号（如 `fix: ... (落实 R-03)`）。

**审查模式（Reviewer）**：**零代码修改、零 commit**。产出审查报告存 `experiments/reviews/<日期_范围>.md`，并在回复正文完整呈现。报告固定结构：
1. 审查范围：文件 / 分支 / commit 哈希；对照的文档与章节。
2. 逐条发现，统一编号 R-01、R-02…，每条含：位置（文件:函数或行）｜类别（正确性 / 与方案一致性 / 数值稳定性 / 性能与显存 / 测试缺口 / 工程）｜问题｜依据（手册或详案的具体章节；纯工程建议标注"工程类，无手册依据"）｜建议改法（具体到可直接执行）｜预期收益｜验证方式｜优先级（P0 阻塞 / P1 应做 / P2 可选）。
3. 汇总：P0/P1 清单与建议落实顺序；涉及云端成本的（显存 / 时长），给出量化影响估计。

发现 P0 也不直接改——报告置顶标注并询问用户是否切换实施模式落实。建议生命周期：报告 → 用户挑选 → 实施模式落实（commit 引用 R-XX）→ PROGRESS.md 勾销。

## 3. 会话启动流程（每次新会话 / 新子任务必做）
1. 读 `experiments/PROGRESS.md`：当前里程碑、阻塞项、交接包队列、上次遗留。
2. 按下表读取对应文档的**相关章节**（用标题定位，不必整读全文）；审查模式按被审查对象所属行取文档，并额外通读手册·实验习惯章节：

| 任务类型 | 必读文档与章节 |
|---|---|
| 环境搭建 / 数据准备 / P1·P2·P3 修复 / 基线 B0 | 《创新点3_实验流程手册_v1》阶段 0 ＋ 里程碑 M0–M2 |
| 顺序敏感性诊断（3a） | 手册·诊断章节 ＋ M3–M4 |
| 顺序对比正则（3b） | 手册·正则章节 ＋ M5–M6（前置：O-MSA 等变性单测结论已明确） |
| 创新点 1（OT 语义评分）实现 / 实验 | 《后续创新点方案设计_v1》创新点 1 详案 ＋ 总调度；实验判据以《创新点1_实验执行清单》为准——该文件按既定决策在 B0 定标后由用户在方案对话中产出并放入 docs/，未产出前若被要求做创新点 1 的实验安排，先与用户确认 |
| 创新点 2 / 4a | 《后续创新点方案设计_v1》对应详案（执行清单后续补） |
| 实验习惯 / 台账 / 风险应对 | 手册·实验习惯与风险清单章节（全程适用，落地方式见 §9） |

3. 文档缺失、或文档与代码实读冲突 → **停下报告用户**，不得自行脑补方案细节。
4. 动手前用两三句复述：**当前模式（实施/审查）**、对应里程碑、已读章节、验收标准。

## 4. 铁律
- **代码事实优先**：写任何接口调用前，先 Read/Grep 源码确认真实文件名、类名、函数签名、张量形状；关键张量在注释中标注形状。文档与代码冲突时以代码实读为准并报告用户。
- **文档只读**：`docs/` 下所有手册与设计文档不修改；进度与结果只写入台账和 PROGRESS.md。
- **审查零修改**：审查模式下不改任何代码、不产生任何 commit；所有改动只在实施模式发生。
- **最小改动**：不重构与当前任务无关的代码，保持与上游 diff 可读。
- **学术诚信红线**：ledger 中只允许来自**云端正式运行**日志的数值；本地 smoke 结果一律不进 ledger、不进论文；日志里解析不出的字段留空，不估算、不外推、不"美化"。
- **正式训练评测只出交接包、不代跑**：本地仅限 §8 定义的验证性训练；交接包内必须附显存/时长/费用估算（首跑可标"待实测"）。

## 5. 已知关键事实（默认成立，除非代码实读推翻）
- 官方**无预训练权重**，基线 B0 必须自训。验收参照论文 63.6（SSv2-Small 5w1s），差距须在可解释范围；排查顺序按手册风险清单：数据 → CLIP 权重与 fp16 路径 → 超参。
- 三处代码隐患（精确位置以手册为准）：
  - **P1** 返回值命名互换：`models.py` 返回 `sem_dists, dists`，`run.py` 端以 `vis_dis, sem_dis` 接收，语义/视觉分数被调换；
  - **P2** 分数矩阵朝向不一致：语义 `(n_way, Q)` vs 视觉 `(Q, n_way)`，当前被 `n_query=1` 掩盖；修复后加形状断言，并用 `n_query>1` 合成单测验证；
  - **P3** CrossAttention 残差与论文式(13)不一致：处理策略按手册执行；手册未明确处询问用户。
  - P1、P2 各自**独立 commit**。**P2 修复导致基线相对论文移动属预期，一切对比以修复后自训的 B0 为对照**，不得直接引用论文数字作对照。
- **O-1**：O-MSA 在子动作轴上疑似排列等变（该轴自注意力无掩码、无位置编码，其余算子逐点作用）。等变性是**架构性质、与权重取值无关**——用随机初始化的真实模块类做 CPU 单测即可定论；云端真实权重复核仅作可选保险项。测试通过前不得使用该结论及"零成本负样本"方案；不通过则按手册回退。
- **O-2**：query 样本在 Task-MSA 与 CrossAttention 中存在 batch 内交互。改动评测 batch 组织方式前必须与用户确认（此事实与创新点 4a 的泄露讨论相关）。
- 环境四坑（云端搭建时发生，写脚本与交接包必须预防；本地装环境同样自查）：两处源码硬编码 CLIP 权重路径；CLIP 不能 `pip install clip`（PyPI 同名包是别的东西，按手册方式安装）；config 缺 `method` 字段会报错；默认 `num_workers` 过大，按机器核数下调。

## 6. 目录与版本约定
```
.                              # 结构对齐上游 Jaulin-Bage/Task-Adapter-pp（2026-08-31 重构）
├── CLAUDE.md
├── run.py models.py dataset.py utils.py   # 官方训练/评测代码（我们的改进就在此，P1/P2 已修）
├── module_adapter.py module_sem_adapter.py # 共享 CLIP 编码器（官方架构+坑B+checkpoint_path 参数）
├── config.yaml                # 上游默认模板；config_b0*/mini/verify.yaml 为我们的配置（TA_CONFIG 加载）
├── corpus/                    # 类名与子动作语料（官方 5 + fsar 2）
├── docs/                      # 手册与设计文档（只读）
├── fsar/                      # 诊断与创新点工具层（import 根 module_adapter 等；自带 fsar.utils/model）
├── configs/                   # fsar 工具层配置
├── data/{mini,verify,full}/   # 本地数据（gitignore；smoke/验证/全量抽帧目录）
├── scripts/
│   ├── tests/                 # CPU 单测（pytest 全量回归；含 tests/official/ 官方代码单测）
│   └── make_mini_subset.py    # 子集抽样脚本
└── experiments/
    ├── handoff/               # 云端交接包，一任务一文件，<日期_代号>.md
    ├── returns/               # 用户带回的云端产物，按同代号建子目录原样存放
    ├── reviews/               # 审查报告，<日期_范围>.md
    ├── ledger.csv             # 主结果台账（唯一权威数据表，只收云端正式运行）
    └── PROGRESS.md            # 跨会话进度
```
- 版本管理：P1、P2 修复各自独立 commit；诊断脚本与各创新点在独立分支开发；每个交接包锁定 commit 哈希；落实审查建议的 commit 引用 R 编号。
-（2026-08-31 结构重构）项目结构对齐上游：**官方训练代码在仓库根目录**（改进直接改根文件），`fsar/` 为诊断与创新点工具层（`import module_adapter`/`module_sem_adapter` 复用根编码器，另有独立 `fsar.utils`/`fsar.model`）；GitHub `official-baseline` 分支 = 纯上游源码（唯一内容，作对照参照）、`main` = 我们的改进；ZSL/CL 轨道在独立仓库 `D:\task-adapter-zslcl`，互不引用。
- 一实验一目录（云端）：沿用仓库按时间戳建目录写 log.txt；`config` 的 `message` 字段填配置代号；config 快照随 returns 带回。
- 进论文的每个数字必须能从台账回溯到 commit ＋ returns 原始日志。
- 一实验一目录（云端）：沿用仓库按时间戳建目录写 log.txt 的机制；`config.yaml` 的 `message` 字段填配置代号；config 快照随 returns 带回。
- 进论文的每个数字必须能从台账回溯到 commit ＋ returns 原始日志。

## 7. 代码与单测规范
- 新增模块独立成文件、最小侵入接入；新超参一律进 config，禁止硬编码。
- 单测是第一道防线（smoke 是第二道）：**合成张量＋随机初始化，CPU 秒级可跑**；覆盖形状正确性、理论性质（不变性/等变性）、梯度存在性；放 `scripts/tests/`。
- 数值稳定性：Sinkhorn/OT 迭代置于 fp32 计算岛（autocast 关闭区），log-domain 实现；创新点 1 超参扫描顺序固定：先 ε（平衡 OT）→ 再 λ → 后 ρ（写进对应交接包）。
- 训练脚本必须支持断点续跑（云实例可能被回收）。
- 语言：文档与交流用中文；代码标识符用英文；实现论文公式处注明公式编号（如 `# 对应式(16)`）。

## 8. 本地验证性训练（smoke）规范
- **定位**：只验证"正确性与通路"，不追求精度；产出不进 ledger、不进论文。
- **数据**：`data/mini/`，仅从**训练划分**抽样（规模建议 8 类 × 8 段左右，最终在交接包①中确定），由 `scripts/make_mini_subset.py` 在云端打包带回。
- **配置**：`config_mini.yaml` 继承正式配置，只缩"数据量与迭代数"（episode/iteration 极小、batch 最小、`num_workers ≤ 2`）；**不缩任何形状相关维度**——帧数、分辨率、子动作数 K 与正式配置保持一致，否则会掩盖形状与对齐类 bug。
- **设备**：首次 smoke 先检测 cuda 可用性并在复述中报告；无 GPU 时把迭代数压到底线并预告耗时。
- **触发时机**：(a) P1/P2/P3 修复合入后；(b) 任何新模块接入训练图后、发对应交接包**之前**——smoke 通过是发包的前置条件；(c) 重大重构后。
- **验收清单**：端到端无报错；loss 有限且若干步内呈下降趋势；全程无 NaN/Inf（fp16 路径重点盯）；checkpoint 保存与 `--resume` 各验证一次；评测脚本能完整出数（数值不作参考）；新增模块梯度非零。
- **结果处置**：PROGRESS.md 登记 `smoke-<代号>：通过 / 失败＋原因`。

## 9. 云端交接协议
**发包（本地 → 云）**：每当改动需要 GPU 或完整数据验证，生成 `experiments/handoff/<日期_代号>.md`，固定包含：
1. 目的与对应里程碑；
2. 分支＋commit 哈希、需同步的文件；对应 smoke 的通过记录（§8 (b) 类必附）；
3. 云端准备步骤（含环境四坑自查清单）；
4. 执行命令（含配置代号）；**首跑类任务必须先列"小规模试跑"步骤，通过后才是全量**；
5. 预计显存 / 时长 / 费用（首跑标"待实测"）；
6. 成功判据（量化、可勾选）；
7. 需带回产物清单：log.txt、config 快照、汇总指标、`nvidia-smi` 显存峰值与墙钟、异常信息全文，以及该包特别指定的附加产物（如交接包①的 mini 子集）。

**收包（云 → 本地）**：用户把产物放入 `experiments/returns/<同代号>/` 后，Claude Code：解析日志 → 对照成功判据逐项勾选 → 结果行写入 `ledger.csv` → 更新 PROGRESS.md → 报告结论与异常（异常排查顺序遵循手册风险清单）。

## 10. 会话收尾（每次必做）
1. 更新 `experiments/PROGRESS.md`（完成 / 进行中 / 阻塞 / 交接包队列 / smoke 记录 / 下一步）。
2. 新回收的结果登账 `ledger.csv`。
3. 回复变更摘要——实施会话：改了哪些文件、为什么、本地如何验证、对应里程碑、是否产出交接包；审查会话：报告路径、P0/P1/P2 计数、建议的落实顺序。
